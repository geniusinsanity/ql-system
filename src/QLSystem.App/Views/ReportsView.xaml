<UserControl x:Class="QLSystem.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Background="#f8fafc" Margin="25">
            <!-- Header with Date Picker -->
            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="لوحة التقارير والأرباح (Rapports &amp; Statistiques)" FontSize="24" FontWeight="Bold" Foreground="#1e293b"/>
                    <TextBlock Text="متابعة رقم الأعمال، المقبوضات الكاش، مبيعات الكريدي، وصافي الأرباح" FontSize="14" Foreground="#64748b" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="اختر اليوم:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding SelectedDate}" Height="38" FontSize="14" VerticalContentAlignment="Center"/>
                    <Button Content="تحديث 🔄" Command="{Binding RefreshCommand}" Style="{StaticResource BtnPrimary}" Height="38" Margin="10,0,0,0" Padding="15,0"/>
                </StackPanel>
            </Grid>

            <!-- 4 Metric Cards in 1 Row -->
            <UniformGrid Columns="4" Margin="0,0,0,25">
                <!-- Total Sales -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="رقم الأعمال (Ventes du jour)" FontSize="13" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Report.TotalSales, StringFormat={}{0:N2} DZD}" FontSize="22" FontWeight="Bold" Foreground="#1e40af" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding Report.TotalTransactions, StringFormat={0} عملية بيع}" FontSize="12" Foreground="#94a3b8" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Cash Collected -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="المقبوضات نقداً (Espèces)" FontSize="13" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Report.CashCollected, StringFormat={}{0:N2} DZD}" FontSize="22" FontWeight="Bold" Foreground="#047857" Margin="0,8,0,0"/>
                        <TextBlock Text="في صندوق الكاسة" FontSize="12" Foreground="#94a3b8" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Credit Sales -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="مبيعات بالكريدي (Crédit)" FontSize="13" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Report.CreditGiven, StringFormat={}{0:N2} DZD}" FontSize="22" FontWeight="Bold" Foreground="#b91c1c" Margin="0,8,0,0"/>
                        <TextBlock Text="ديون جديدة للزبائن" FontSize="12" Foreground="#94a3b8" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Net Profit -->
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <TextBlock Text="صافي الفائدة المقدرة (Bénéfice)" FontSize="13" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Report.NetProfit, StringFormat={}{0:N2} DZD}" FontSize="22" FontWeight="Bold" Foreground="#0284c7" Margin="0,8,0,0"/>
                        <TextBlock Text="فرق البيع عن الشراء" FontSize="12" Foreground="#94a3b8" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Secondary Metrics -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="تسديدات الديون المستلمة اليوم (Recouvrement)" FontSize="16" FontWeight="Bold" Foreground="#1e293b" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding Report.DebtsCollected, StringFormat={}{0:N2} دج}" FontSize="20" FontWeight="Bold" Foreground="#10b981"/>
                        <TextBlock Text="مبالغ سددها الزبائن من ديونهم السابقة" FontSize="13" Foreground="#64748b" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <TextBlock Text="حالة المخزون وتنبيهات النقص" FontSize="16" FontWeight="Bold" Foreground="#1e293b" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding Report.LowStockAlertsCount, StringFormat={0} سلعة قاربت على النفاد}" FontSize="20" FontWeight="Bold" Foreground="#d97706"/>
                        <TextBlock Text="يرجى طلبها من الموردين لتفادي انقطاع السلعة" FontSize="13" Foreground="#64748b" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
