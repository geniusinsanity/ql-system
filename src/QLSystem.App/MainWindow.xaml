<Window x:Class="QLSystem.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QLSystem.App.ViewModels"
        xmlns:views="clr-namespace:QLSystem.App.Views"
        mc:Ignorable="d"
        Title="QL System - برنامج تسيير محلات الكانكري والعتاد (Quincaillerie Algérie)" 
        Height="780" Width="1280" MinHeight="650" MinWidth="1024"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#0f172a"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- DataTemplates mapping ViewModels to Views -->
            <DataTemplate DataType="{x:Type vm:CaisseViewModel}">
                <views:CaisseView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:StockViewModel}">
                <views:StockView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CreditViewModel}">
                <views:CreditView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReportsViewModel}">
                <views:ReportsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TrialLockViewModel}">
                <views:TrialLockView/>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Navigation Sidebar (Hidden if locked) -->
            <ColumnDefinition Width="240">
                <ColumnDefinition.Style>
                    <Style TargetType="ColumnDefinition">
                        <Setter Property="Width" Value="240"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                <Setter Property="Width" Value="0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
            </ColumnDefinition>
            <!-- Main Content Area -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#0f172a" BorderBrush="#1e293b" BorderThickness="0,0,1,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brand Logo & Title -->
                <StackPanel Grid.Row="0" Margin="5,10,0,30">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🛠️" FontSize="26" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="QL SYSTEM" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Quincaillerie Management" FontSize="11" Foreground="#64748b" Margin="36,2,0,0"/>
                </StackPanel>

                <!-- Navigation Menu Buttons -->
                <StackPanel Grid.Row="1">
                    <Button Content="🛒  الكاسة والبيع (Caisse)" Command="{Binding NavigateCaisseCommand}" Style="{StaticResource BtnNav}"/>
                    <Button Content="📦  المخزون والسلع (Stock)" Command="{Binding NavigateStockCommand}" Style="{StaticResource BtnNav}"/>
                    <Button Content="👥  ديون الزبائن (Crédit)" Command="{Binding NavigateCreditCommand}" Style="{StaticResource BtnNav}"/>
                    <Button Content="📊  التقارير والأرباح (Rapports)" Command="{Binding NavigateReportsCommand}" Style="{StaticResource BtnNav}"/>
                    <Button Content="⚙️  الإعدادات (Paramètres)" Command="{Binding NavigateSettingsCommand}" Style="{StaticResource BtnNav}"/>
                </StackPanel>

                <!-- Footer: Version & Machine ID -->
                <StackPanel Grid.Row="2" Margin="5,0,0,10">
                    <TextBlock Text="نسخة حاسوب واحد (Monoposte)" FontSize="11" Foreground="#475569"/>
                    <TextBlock Text="{Binding LicenseInfo.MachineId}" FontSize="10" Foreground="#334155" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="v1.0.0 DZ • 2026" FontSize="11" Foreground="#64748b" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Body -->
        <Grid Grid.Column="1" Background="#f8fafc">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header Bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1" Padding="25,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="نظام تسيير محلات الكانكري والعتاد" FontSize="15" FontWeight="SemiBold" Foreground="#334155" VerticalAlignment="Center"/>

                    <!-- License Status Badge -->
                    <Border Grid.Column="1" Background="{Binding LicenseBadgeColor}" CornerRadius="20" Padding="14,6">
                        <TextBlock Text="{Binding LicenseBadgeText}" Foreground="White" FontSize="13" FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Active View Content -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</Window>
